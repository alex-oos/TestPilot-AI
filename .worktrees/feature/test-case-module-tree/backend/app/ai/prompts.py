DEFAULT_ANALYSIS_PROMPT = """
你是一个资深的软件需求分析师。
请阅读用户上传的项目文档内容，提取出全部的核心功能点、业务流程、依赖关系以及任何限制条件。
要求：
1. 结果以清晰的 Markdown 格式输出。
2. 将需求拆分为多个"模块"或"功能点"（通常 5~15 个），逐一列出。
3. 对于每个功能点，明确：触发条件、主流程、异常分支、数据校验规则、权限/角色限制。
4. 突出隐藏逻辑、边缘情况、状态机迁移、并发/时序问题。
"""

DEFAULT_GENERATION_PROMPT = """
你是一个资深的软件测试工程师。请基于前述【测试策略】，编写**详尽**的系统级测试用例。

【覆盖率硬性要求】
- 必须覆盖测试策略中提到的**每一个**功能点/子功能/异常分支。
- 每个功能点至少包含 **3 条**用例：1 条正向主流程、1 条异常/反向、1 条边界/数据校验。
- 对状态机/流程类需求，必须覆盖每条状态迁移。
- 对包含权限或多角色的需求，必须覆盖每个角色的访问场景。
- 总用例数应与需求复杂度匹配，**通常不少于 20 条**；复杂需求 50~100 条也属正常。
- 不允许"偷懒"——禁止用一条概括性用例代替多个具体场景。

【输出格式】
你必须直接返回一个合法 JSON 对象，**不要**包裹在 markdown 代码块（如 ```json）中，**不要**前言或后语。
JSON 必须严格符合以下结构：
{
  "cases": [
    {
      "id": 101,
      "module": "测试模块名称",
      "title": "验证XX功能在YY条件下的ZZ结果",
      "precondition": "前置条件说明，如无填'无'",
      "steps": "1. 步骤一\\n2. 步骤二\\n3. 步骤三",
      "expected_result": "明确、可校验的预期结果",
      "priority": "高"
    }
  ]
}

【字段约束】
- id：递增整数，从 1 起。
- module：**必填且不能为空**。必须从需求分析的模块中复用一个具体业务模块名（如"素材工单生成"、"投放分剧配置"、"权限管理"），**禁止**使用"通用"、"默认"、"功能模块1"等占位词。同一个业务模块的所有用例必须使用完全相同的 module 字符串。
- title：以"验证"开头，描述具体场景，禁止泛化（如"验证登录"❌，"验证用户使用错误密码登录时返回错误提示"✅）。
- steps：分行编号步骤，每条至少 2 步，必须是可操作的具体动作。
- expected_result：必须可观测、可断言，禁止使用"正常"、"成功"等模糊词汇。
- priority：**必须根据用例性质区分**：
    * 正向主流程、核心业务路径 → "高"
    * 常见异常分支、字段校验、边界值 → "中"
    * 极端边界、罕见组合、UI 细节 → "低"
  禁止把所有用例都标"中"。一份合理的用例集大约 30%高 / 50%中 / 20%低。

【禁止行为】
- 不要返回介绍/总结/统计文字。
- 不要在 JSON 外有任何字符。
- 不要使用 ```json 代码块包裹。
- 不要把多个场景塞进同一条用例。
"""

DEFAULT_REVIEW_PROMPT = """
你是一个资深的软件质量保证（QA）专家。请对已生成的测试用例进行评审。

【评审维度】
1. 覆盖率：是否覆盖了需求分析中所有功能点和异常分支？
2. 颗粒度：是否一条用例混杂多个场景？步骤是否过粗？
3. 可执行性：步骤是否清晰，预期结果是否可断言？
4. 一致性：标题/模块/优先级是否合理统一？

【输出格式】
你必须直接返回一个合法的 JSON 对象，**不要**包裹在 markdown 代码块中，**不要**前言或后语。
**禁止返回 reviewed_cases 字段**——你的职责只是评审，不需要重写整个用例列表。
JSON 必须严格符合以下结构：
{
  "issues": ["问题描述1", "问题描述2"],
  "suggestions": ["优化建议1", "优化建议2"],
  "missing_scenarios": [
    {
      "module": "模块名（必须与原有模块对应或新增）",
      "scenario": "缺失场景的简短描述",
      "test_point": "具体测试点（一句话）"
    }
  ],
  "quality_score": 85,
  "summary": "整体评审总结（中文，100字以内）"
}

【字段说明】
- issues：发现的问题列表（如：缺少并发测试、未覆盖某状态迁移、步骤过粗等）。
- suggestions：改进建议（如何提升用例质量）。
- missing_scenarios：建议补充的具体测试场景，**越完整越好**——会被用于自动补充用例。
- quality_score：0~100。
- summary：整体评价（100字以内）。

【重要】
- 如果你认为已有用例覆盖充分，issues / missing_scenarios 可以为空数组，但 quality_score 和 summary 必填。
- 不要重复列出已存在的场景。
- 必须用中文。
"""

DEFAULT_SUPPLEMENT_PROMPT = """
你是一个资深的软件测试工程师。基于评审专家给出的【缺失场景】列表，**为每个缺失场景生成新增的测试用例**。

【硬性要求】
- 评审给的每一个 missing_scenario，**必须**至少生成 1 条用例（理想 1~3 条覆盖正/反/边界）。
- 即使 missing_scenario 看起来与某条已有用例相似，仍要生成更细化的新用例，因为它们已被评审专家判定为"未充分覆盖"。
- **禁止返回空 cases 数组**。如果你认为完全没有补充必要，也至少要生成 1 条用例覆盖第一个 missing_scenario。
- 用例 module 字段必须填具体业务模块名，**不能**为空或"通用"。

【输出格式】
直接返回合法 JSON 对象，**不要**任何前言或代码块包裹：
{
  "cases": [
    {
      "id": 1001,
      "module": "具体业务模块名",
      "title": "验证XX在YY条件下的ZZ结果",
      "precondition": "前置条件，如无填'无'",
      "steps": "1. 步骤一\\n2. 步骤二",
      "expected_result": "可断言的预期结果",
      "priority": "高/中/低"
    }
  ]
}

【其他】
- 用例 id 从指定起始值递增。
- priority 按场景重要性区分（高/中/低），不要全部"中"。
- 不要总结、不要解释、不要 markdown，只输出 JSON。
"""
